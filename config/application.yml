default: &default
  # # MailGun
  # From address
  MAILGUN_FROM_ADDRESS: "Globetutoring <info@mg.globetutoring.com>"
  ROOT_URL: "http://globetutoring.com/"
  DATA_URL: "http://localhost:3000/"
  # DATA_URL: "https://i.doers.io/"

  # Payments
  PAYPAL_RETURN_PATH: /payments/authorized
  PAYPAL_CANCEL_PATH: /payments/cancelled

development:
  <<: *default

test:
  <<: *default

production:
  <<: *default
